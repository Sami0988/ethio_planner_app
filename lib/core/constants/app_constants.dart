class AppConstants {
  AppConstants._();

  static const String appName = 'Ethio Planner';
  static const String baseUrl = 'https://api.ethioplanner.com';

  static const Duration networkTimeout = Duration(seconds: 30);
  static const Duration cacheDuration = Duration(minutes: 5);
}
