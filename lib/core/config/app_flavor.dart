/// Build flavors mapped to separate application IDs, API base URLs, Firebase
/// projects, monitoring environments and logging levels (SRS §69.1).
enum AppFlavor {
  development,
  staging,
  production,
}
